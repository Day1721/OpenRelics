using System.Text.RegularExpressions;

namespace OpenRelicsWebApp.Models
{/*
    public static class DatingParcer
    {
        public static IDateInterval Parse(this string dating)
        {
            if (string.IsNullOrEmpty(dating))
                return null;

            var match = Regex.Match(dating, "^[XIV]{1,3}$");
            if (match.Success)
            {
                
            }
        }
    }

    public interface IDateInterval
    {
        int Begin { get; }

        int End { get; }
    }

    public class Interval : IDateInterval
    {
        public Interval(int begin, int end)
        {
            Begin = begin;
            End = end;
        }

        public int Begin { get; }
        public int End { get; }
    }

    public class SingleYearInterval : IDateInterval
    {
        private readonly int _year;

        public SingleYearInterval(int year)
        {
            _year = year;
        }

        public int Begin => _year;
        public int End => _year;
    }*/
}