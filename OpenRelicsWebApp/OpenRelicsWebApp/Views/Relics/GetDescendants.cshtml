@model List<int>

@if (Model == null)
{
    <h2>There is no relic with given id</h2>
}
else
{
    <ul>
        @foreach (var relic in Model)
        {
            <li>@relic</li>
        }
    </ul>
    
}

@Html.ActionLink("Back", "Index")